import { TranslateLanguageLoader } from './translate-language-loader';

describe('TranslateLanguageLoader', () => {
  it('should create an instance', () => {
    expect(new TranslateLanguageLoader()).toBeTruthy();
  });
});
